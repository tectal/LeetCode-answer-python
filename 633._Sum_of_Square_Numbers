from math import sqrt
class Solution:
    def judgeSquareSum(self, c):
        """
        :type c: int
        :rtype: bool
        """
        a = 0
        while a*a <= c:
            if in(sqrt(c - a*a)) == sqrt(c - a*a):
                return True
            a += 1
        return False
